.env
**/.azure/

# Bicep compiled outputs (ARM JSON templates)
# Only exclude JSON files that share the same base name as a Bicep file
# e.g., octopets-az-monitor-alerts.json is excluded if octopets-az-monitor-alerts.bicep exists
demos/AzureHealthCheck/*.json
demos/ServiceNowAzureResourceHandler/octopets-service-now-alerts.json
# Keep ProactiveReliabilityAppService JSON files (they are intentional config/templates)

# Instance-specific project memory (do not commit)
.docs/memory.md

# Instance-specific working memory (do not commit)
memory.md

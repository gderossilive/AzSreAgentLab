# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: grocery-sre-demo
metadata:
  template: grocery-sre-demo@0.0.1

services:
  api:
    project: ./src/api
    language: js
    host: containerapp

  web:
    project: ./src/web
    language: js
    host: containerapp
